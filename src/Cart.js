import { Card, Button } from 'antd';

const Cart = () => {
  return (
    <Card
     
      style={{
        width: 300,
        height: 250,
        backgroundImage: 'linear-gradient(to right, #667eea, #764ba2)',
        borderRadius: 8,
        padding: '20px 50px 50px', // Adjusted padding values
        color: 'white',
        display: 'flex',
        margin:"40px",
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
    </Card>
  );
};

export default Cart;
